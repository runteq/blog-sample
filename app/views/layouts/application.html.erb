<!DOCTYPE html>
<html>
  <head>
    <title>らんてくんのブログ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen flex flex-col">
    <%= render "shared/header", title: "らんてくんのブログ" %>

    <main class="container mx-auto py-8 px-5 flex-grow">
      <% if notice %>
        <div class="max-w-4xl mx-auto mb-6 bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg">
          <%= notice %>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <footer class="text-center text-lg font-bold text-stone-500 py-6">
      <%= link_to "プロフィール", profile_path, class: "text-sky-600 hover:text-sky-800 mr-4" %>
      ©︎ 2024 RUNTEQ
    </footer>
  </body>
</html>
